apiVersion: v1
kind: Service
metadata:
  name: customer-frontend
  namespace: mbd
spec:
  selector:
    app: customer-frontend
  ports:
    - port: 80
      targetPort: 80
      name: http
  type: ClusterIP
