apiVersion: v1
kind: Service
metadata:
  name: admin-frontend
  namespace: mbd
spec:
  selector:
    app: admin-frontend
  ports:
    - port: 80
      targetPort: 80
      name: http
  type: ClusterIP
