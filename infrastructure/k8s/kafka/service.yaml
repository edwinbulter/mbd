apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: mbd-infra
spec:
  selector:
    app: kafka
  ports:
    - port: 9092
      targetPort: 9092
      name: plaintext
    - port: 9093
      targetPort: 9093
      name: broker
  type: ClusterIP
  clusterIP: None
