apiVersion: v1
kind: ResourceQuota
metadata:
  name: mbd-quota
  namespace: mbd
spec:
  hard:
    requests.cpu: "8"
    requests.memory: 16Gi
    limits.cpu: "24"
    limits.memory: 32Gi
    persistentvolumeclaims: "20"
